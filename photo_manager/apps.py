"""Photo Manager APP Config."""
from django.apps import AppConfig


class PhotoManagerConfig(AppConfig):
    """Photo Manager Config."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'photo_manager'
